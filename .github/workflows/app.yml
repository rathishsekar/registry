name: Registry App CI

on:
  push:
    branches : [master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ./gradlew build
      - run: ./gradlew test
