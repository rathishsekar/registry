name: Registry App CI
on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: build
        run: ./gradlew build
      - name: test
        run: ./gradlew test
